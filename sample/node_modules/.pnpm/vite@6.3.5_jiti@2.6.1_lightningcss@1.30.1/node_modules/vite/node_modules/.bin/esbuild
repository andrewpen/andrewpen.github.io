#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

"$basedir/../../../esbuild/bin/esbuild"   "$@"
exit $?
# cmd-shim-target=/Users/pendlan/codebook/andrewpen.github.io/sample/node_modules/.pnpm/vite@6.3.5_jiti@2.6.1_lightningcss@1.30.1/node_modules/esbuild/bin/esbuild
